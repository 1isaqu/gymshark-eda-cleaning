name: deploy dashboard

# Builds dashboard/ and publishes it to GitHub Pages at
# https://1isaqu.github.io/gymshark-eda-cleaning/
#
# The site is a static Vite build. It reads no secrets and talks to no
# backend: all figures come from the committed dashboard/src/data/findings.json,
# and the product photos are loaded straight from Shopify's CDN at view time.

on:
  push:
    branches: [main]
    paths:
      - "dashboard/**"
      - ".github/workflows/deploy.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Never let two deploys race. Queue instead of cancelling, so a push that
# lands mid-deploy still ends up published rather than silently dropped.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: dashboard
    steps:
      - uses: actions/checkout@v7

      # Matches the local dev machine (24.x), so a build that passes here
      # and a build that passes locally are running the same runtime.
      - uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: dashboard/package-lock.json

      - name: install
        run: npm ci

      # Same gates that guard local work, so a broken build fails here
      # rather than shipping to the live URL.
      - name: typecheck
        run: npx tsc --noEmit

      - name: build
        run: npm run build

      - uses: actions/configure-pages@v6

      # path is relative to the workspace root, not to `working-directory`.
      #
      # v4 of this action stopped including dotfiles in the artifact. The
      # Vite build emits none (just index.html and assets/), so nothing is
      # lost here. If that ever changes, v5 has an `include-hidden-files`
      # input rather than needing a pin back to v3.
      - uses: actions/upload-pages-artifact@v5
        with:
          path: dashboard/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
